# Agent Notes

- The workspace path includes spaces (e.g., `C:\My Data\...`). When using patch/edit tools or shell commands, always use correct workspace-relative paths and quote any paths with spaces to avoid failures.
